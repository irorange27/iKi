= 系统需求分析与总体设计

== 应用场景分析

本文系统面向的并不是单纯的聊天娱乐场景，而是“任务协同与情绪波动并存”的桌面使用环境。用户在电脑前处理工作、学习或生活事务时，往往既需要具体帮助，又会受到自身状态的影响。例如，在准备向经理汇报延期进度时，用户可能因羞耻或回避而难以下手；在论文修改、客户回复、职业选择等任务中，用户也可能因为信息不足、过载或边界压力而陷入停滞。此时，一个只追求最快给答案的系统，并不一定能真正帮助用户推进任务。

因此，本文把系统的主要应用场景划分为三类。第一类是中性生产力场景，例如改写文字、生成清单、整理文档、归纳问题单等，这类任务更关注完成效率和输出可用性。第二类是情绪与任务纠缠场景，例如困难沟通、拖延重启、信息不足但要求“直接给答案”等，这类任务更需要系统正确选择干预强度。第三类是桌面持续伴随场景，要求系统在非全屏聊天状态下仍然能够通过 companion 形式反馈自身状态，形成“低打扰但可见”的在场感。

围绕这三类场景，系统不仅要能够“回答”，还要能够“判断当前适合怎么回答”。这也是本文从一开始就把 affect-aware policy 放入整体架构，而不是后补为附加模块的原因。

== 功能需求分析

根据上述场景，本文系统至少需要满足以下功能需求。

+ 支持基础多轮对话。系统应能够在桌面端接收用户输入、维护线程上下文、显示流式回复，并为后续状态更新提供稳定的交互入口。

+ 支持多模型与多提供商接入。系统应能够连接不同模型提供商，并按运行时配置选择主对话模型、辅助模型与工具执行环境。

+ 支持 affect 状态分析与聚合。系统需要对用户消息进行情绪分析，并将多轮样本聚合为结构化状态，以便为当前轮决策提供输入。

+ 支持显式干预策略控制。系统应能够根据 affect 状态、任务目标与上下文完整度，在 `stabilize`、`clarify`、`co_plan`、`guided_execute` 和 `autonomous_execute` 等模式之间选择合适动作。

+ 支持记忆与上下文组装。系统应能在适当范围内检索线程摘要、长期记忆与技能元数据，并以结构化方式纳入上下文。

+ 支持工具与技能路由。系统应能够在需要时调用本地工具、技能或代理子任务，同时在高风险状态下降低自治程度。

+ 支持桌面宠物 companion 外显反馈。系统应提供独立于主聊天界面的 companion 视图，以显示当前运行状态、思考阶段和关键提醒。

+ 支持实验评测与数据管理。系统应提供与论文实验相匹配的数据集、脚本和结果汇总路径，使系统层评估和机制层评估都可复现。

== 非功能需求分析

除了功能可用性，本文系统还必须满足若干非功能需求。

首先是可审计性。由于本文研究的是 affect 是否影响 agent 决策，因此系统需要让 affect 信号、上下文组装结果、策略状态与工具 guardrail 具有明确的数据表达，而不能把所有逻辑都隐藏在 prompt 文本之中。

其次是边界安全性。系统运行在本地桌面环境中，涉及文件、命令和模型调用，因此必须对工作区范围、工具审批和隐私模式进行严格控制。在用户高唤醒、低价度或边界风险较高的状态下，系统还需要能够主动降低自治程度。

再次是可扩展性。本文系统不应只适配某一个模型或某一个界面，而应具有多模型、多入口、多能力模块并存的长期形态。这要求系统架构能够清晰区分控制平面、运行时 authority、网关层和能力 harness。

最后是响应效率与低打扰体验。桌面场景要求系统在保持上下文丰富度的同时尽量控制响应延迟，并通过轻量 companion 反馈而不是高频打断来实现持续在场感。

== 系统总体架构设计

结合代码库中的当前实现，本文将系统总体架构概括为“控制平面 + 运行时 authority + 能力 harness + 网关层”的分层结构。

最上层是桌面控制平面，由 Electron 主窗口、设置页面与 companion 视图组成，负责配置、检查、交互与可视反馈。中间层是共享运行时 authority，主要负责对话准备、上下文组装、审批恢复、记忆读写、任务管理与状态持久化，是系统真正的行为中枢。其下是能力 harness，封装本地工具、技能、MCP 能力、记忆子系统、连续性信息与主动任务等能力。最外侧是网关层，用于连接不同的人机入口或外部桥接方式，例如桌面 renderer、IPC 桥和守护进程接口。

从论文视角看，系统的核心执行链路可以进一步抽象为五个阶段：感知、状态建模、决策、执行与外显反馈。感知阶段接收用户输入并更新线程；状态建模阶段形成 affect 状态与任务上下文；决策阶段选择当前轮干预模式；执行阶段调用模型生成回复并在必要时触发工具；外显反馈阶段则把内部状态映射到主界面或桌宠 companion。

这样的架构有两个优势：一是它允许 affect 成为贯穿式信号，而不是局部 side feature；二是它把“聊天能力”“工具能力”“桌宠表现”统一到同一运行时语义中，使论文题目的“桌面宠物交互系统”真正形成闭环。

== 核心业务流程设计

系统的核心业务流程可描述如下。

首先，用户在桌面界面发起输入，系统将消息写入线程并触发当前轮准备逻辑。随后，情绪分析模块对用户消息进行异步分析，已有历史情绪事件则参与 affect 聚合，形成当前线程可用的 affect 状态。

其次，上下文组装器根据模型预算和运行配置，决定是否注入线程摘要、记忆、技能元数据与 affect 块，并生成结构化的上下文报告。与此同时，策略层结合 affect 状态、任务上下文和对话阶段，判断当前轮应采用的干预模式。

然后，主对话模型在上述上下文与策略约束下生成回复；如需工具、技能或其他能力，系统再通过路由模块决定是否执行，以及在当前状态下是否需要用户审批。执行完成后，系统更新线程元数据、记录 affect 信号和运行时状态，并将 companion phase 切换为新的可见反馈。

这个流程既覆盖了普通任务中的标准执行路径，也为高风险或高不确定场景提供了“先稳定、先澄清、再推进”的控制入口。

== 数据组织与模块接口设计

为了支撑系统运行与论文实验，本文系统在数据组织上区分了运行时数据与实验数据两类。

运行时数据主要包括线程消息、长期记忆、情绪事件、聚合后的 affect 状态、运行时 metadata 以及主动任务信息。其中，情绪事件表用于记录与消息关联的结构化情绪分析结果，affect 状态表保存线程级最新聚合状态；这些数据不会直接暴露用户消息原文，从而降低隐私风险。

实验数据则主要保存在 thesis benchmark 目录中，包括 `cases.jsonl`、`gold.jsonl`、manifest、annotation 记录、selection log 和 rewrite note 等资产。其组织方式遵循“样本输入与构造元信息分离”的原则：系统在运行时只能看到标准输入接口，而研究者仍可追溯样本的构造来源、控制变量和重写记录。

模块接口方面，本文强调结构化而非隐式耦合。affect 通过统一的 `AffectSignal` 进入上下文装配、工具路由与 UI 透明层；策略层通过有限动作空间输出 `intervention_state` 与 `escalate` 等控制字段；实验接口则通过统一脚本输出预测结果、逐条分析和汇总文件。这种做法有助于后续进行消融、回归测试和论文复现。

== 本章小结

本章从应用场景、功能需求、非功能需求、系统架构、核心流程和数据组织等方面，对基于 Agent 的桌面宠物交互系统进行了总体设计。分析结果表明，本文系统的重点并不在单一模块的局部优化，而在于把 affect、记忆、工具和桌宠外显层统一到一个可审计、可扩展的运行时框架中。下一章将在此基础上进一步介绍情感感知驱动的智能体决策机制及其关键实现。
