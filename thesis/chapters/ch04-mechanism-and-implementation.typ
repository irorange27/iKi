= 情感感知驱动的智能体决策机制设计与系统实现

== 系统运行链路设计

本文系统的运行链路以“感知 -> 状态建模 -> 决策 -> 执行 -> 外显反馈”为主线展开。与传统聊天式应用不同，本系统并不把模型调用视为唯一核心，而是把当前轮对话看作一个由多个状态转换步骤组成的执行过程。

在感知阶段，系统接收用户消息、维护线程状态并更新必要的运行元信息。进入状态建模阶段后，系统对用户消息的情绪线索进行分析，将历史情绪事件聚合为 affect 状态，同时结合近期对话、线程摘要和相关记忆形成更完整的上下文表示。

在决策阶段，系统并不直接要求模型“自由发挥”，而是先由显式策略层根据 affect、任务状态和上下文完整度确定当前轮的干预模式。执行阶段再由主对话模型在该模式的约束下生成回复，并按需要调用工具或技能。最后，外显反馈阶段把内部 phase 投射到桌面 companion，使用户能够在不展开主窗口的情况下感知系统当前的工作姿态。

这种链路的关键意义在于：它让情感信号不是在最后阶段“润色语气”，而是在前置的状态建模和决策阶段就发挥作用，从而真正影响 agent 的行动方式。

== 情感状态表示与更新机制

为避免把系统写成“识别用户真实心理状态”的强断言，本文将 affect 明确定义为带不确定性的运行时 belief，而不是 ground truth。当前实现中，affect 的最小表示至少包含主标签、置信度、价度、唤醒度、标签分布、采样窗口和来源信息等字段，并由统一的 `AffectSignal` 在运行时传播。

系统首先在用户消息入库后触发情绪分析，将结构化结果保存到情绪事件表中。随后，在生成下一轮回复时，聚合器从最近时间窗内选取若干样本，结合最小样本数、最小置信度、是否纳入中性样本等配置进行过滤，并使用时间衰减对较旧样本降权。若当前线程暂无足够情绪事件，系统也可以在受配置约束的情况下从已有短期记忆中回退获取相关信息，以降低升级后的冷启动问题。

为了服务不同用途，AffectSignal 除了保存状态本身，还额外保存信号来源和 guard 是否激活。这使同一个 affect 对象能够同时被上下文注入、工具路由、线程 metadata 与 UI 透明层复用，而不必在各模块中重复推断。该设计提升了运行时一致性，也为后续实验中的“affect 是否真实进入决策链路”提供了审计依据。

== 显式干预策略模型设计

本文的核心机制是显式干预策略层。该层不直接生成完整回复，而是根据 affect 状态、任务目标、对话阶段和信息完备度等因素，先判断“系统当前该以怎样的姿态介入”。

结合现有实现和论文主线，本文使用的最小动作集合包括五种模式：`stabilize`、`clarify`、`co_plan`、`guided_execute` 和 `autonomous_execute`。其中，`stabilize` 用于高压或明显失稳状态下的低刺激承接与边界保护；`clarify` 用于目标、约束或决策变量尚不充分明确的场景；`co_plan` 用于用户需要较低门槛、共同行动启动的场景；`guided_execute` 用于目标明确但仍需分步支撑的场景；`autonomous_execute` 则用于低风险、信息充分且用户已准备好推进的场景。

这一动作空间的价值在于，它把“agent 应该做什么”从模糊的 prompt 风格偏好，转化为有限、可审计、可消融的状态机输出。后续实验中，系统将围绕这些动作状态进行自动评分与对比分析，从而验证 affect 是否真的改变了干预模式，而不仅仅改变了措辞。

== 记忆检索与上下文注入实现

当系统进入具体生成阶段时，必须把多种上下文信息压缩到有限的模型输入预算内。为此，本文所依赖的实现采用统一的 context assembler，将上下文分为若干逻辑块，包括基础系统提示、线程摘要、记忆块、affect 块、技能块和近期原始对话等。

与简单的字符串拼接不同，context assembler 会为每个块记录状态、估计长度和裁剪原因，并根据模型能力元数据和输出保留空间进行动态预算分配。对于长线程，系统优先保留近期对话细节，同时将较早消息压缩为滚动摘要；对于记忆检索，系统优先考虑线程内和同一边界内的相关信息；对于 affect，则在有足够可信样本时注入结构化状态块，以指导当轮节奏和干预深度。

该实现使上下文注入从“隐式提示工程”变为“可观测的装配过程”。这对于论文非常重要，因为 affect 只有在能够被清晰证明进入上下文与决策链路时，才构成真正的系统贡献。

== 工具路由与任务执行控制实现

在工具路由层，系统不仅要判断“需不需要工具”，还要判断“在当前状态下系统是否适合拥有过高自治”。这意味着 affect 会同时影响工具选择姿态和审批边界。

本文系统中，工具与技能路由都会显式接收 affect 信号，而不是要求路由模型从对话文本中间接猜测用户状态。这样做有三个好处：第一，生成模型和路由模型看到的是同一份 affect 快照，降低了状态漂移；第二，系统可以基于明确定义的唤醒度和价度阈值激活 guardrail，而不是依赖粗糙标签启发式；第三，实验中可以更容易区分“只影响语气”与“真实影响执行控制”的条件差异。

在高唤醒、低价度状态下，系统可以降低工具自治程度，例如禁用自动工具、强制审批或仅保留更保守的执行路径。这样一来，agent 的安全边界就不再只依赖主模型的隐式判断，而能够由系统层明确约束。这也是 persistent agent 场景下比单轮聊天系统更重要的能力。

== 桌面宠物反馈表现机制实现

如果 affect 与 policy 只存在于内部 metadata 和系统提示中，那么“桌面宠物交互系统”的题目仍然是不完整的。因此，本文进一步将内部决策姿态映射到独立的 companion 视图，使桌面层也能感知系统状态。

当前实现采用轻量 `companion pebble` 形态，而不是复杂的 Live2D 或自由行动角色。其核心 phase 包括 `thinking`、`stabilize`、`clarify`、`co_plan`、`execute`、`nudge` 和 `idle` 等，由主进程统一裁决并映射为固定文案和低刺激视觉反馈。这样做的目的并不是追求强人格化，而是让桌宠成为运行时状态的稳定外显层。

当系统处于思考或工具执行阶段时，桌宠能够体现“正在工作”的姿态；当系统判断当前轮需要先稳定状态、先澄清问题或共同规划时，桌宠也会切换到更克制的反馈模式。该设计使 affect-aware policy 不再只是论文中的抽象概念，而能在桌面交互层被用户直接感知。

== 关键模块实现小结

本章围绕 affect-aware policy 的实现链路，对系统的关键模块进行了说明。整体来看，本文的实现并不是给现有聊天系统额外加一个“情绪标签”，而是把 affect 作为统一结构化信号嵌入状态聚合、上下文组装、干预策略、工具边界和桌宠外显层之中。通过这种方式，系统才真正具备“依据用户状态调整行动方式”的能力，也为后续实验中验证显式策略层的必要性打下了实现基础。
