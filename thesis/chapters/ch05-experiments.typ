= 实验设计与结果分析

本章在当前初稿中已经完成实验设计、数据集说明和结果分析框架的整理。由于正式跑表结果尚未统一回填，因此本章中的“结果数值”和“结果表格”仍需在完成 benchmark 运行后补入，但整体实验逻辑已经与仓库内的 thesis 资产保持一致。

== 实验目标与研究问题

本文实验围绕三个研究问题展开。

研究问题一是：显式情感感知策略是否能够减少情绪与任务纠缠场景下的不当干预。本文在这里关注的不是系统是否“更会聊天”，而是它是否更少在不合适的时候推进任务、替用户做决定或越过边界。

研究问题二是：在引入显式策略层后，系统在中性生产力任务上的帮助能力是否不会明显下降。也就是说，affect-aware policy 的收益不能建立在普遍变保守、普遍降低任务效用的代价上。

研究问题三是：情感信号是否真实改变了智能体的干预模式选择，而不是只改变回复语气。如果不同 affect 条件下系统能够稳定切换 `clarify`、`co_plan`、`guided_execute` 与 `autonomous_execute` 等状态，那么才说明显式策略层具有独立价值。

== 实验环境与对比条件

为保证实验结论具有可解释性，本文采用三种运行条件进行比较：`no_affect`、`tone_only` 与 `explicit_policy`。

在 `no_affect` 条件下，系统不向运行时注入 affect 状态，也不让 affect 参与显式策略控制。该条件表示不使用情感信号的基线系统。

在 `tone_only` 条件下，系统允许 affect 进入系统上下文，但不让它真正控制显式干预策略。该条件对应“情感只影响语气或上下文，不影响实际行为控制”的中间基线。

在 `explicit_policy` 条件下，系统显式根据 affect 相关状态生成干预策略，并将该策略注入本轮生成过程。该条件对应本文的核心方法。

三种条件在当前 benchmark 脚本中具有清晰、可审计的运行语义，且会额外输出 `runtime_policy_applied` 与 `runtime_policy_affect_used` 等字段，用于确认实验条件是否按预期生效。正式实验时，还需要在本节补入主模型版本、辅助模型配置、运行日期、机器环境与是否启用 profile-latency 等信息。

== 论文数据集构造与质量控制

本文使用的数据集由主测试集和中性对照集两部分组成。主测试集包含 `40` 条样本，由 `8` 个基础任务各自扩展出 `5` 个变体；中性对照集包含 `12` 条样本，用于检验显式策略层是否对普通任务辅助能力造成明显损害。

主测试集的五类变体分别是 `ready_execute`、`blocked_by_affect`、`ambiguous_need`、`boundary_or_escalate` 和 `underspecified_execute`。这些变体并不是靠简单替换情绪词得到的，而是通过改变准备度、信息充分度、边界风险和情绪负载等关键变量构造的。这样设计的好处在于，系统必须真正理解“当前更需要承接、澄清还是执行”，而不能只靠关键词猜答案。

数据集构造流程采用“先冻结任务结构，再写入表面文本”的思路。研究者首先通过 manifest 固定任务骨架和预期干预倾向，再生成用于系统输入的 `history`、`current_user_message` 与 `task_context`。为降低标签泄漏风险，数据集还经过显式 cue audit，并对部分线索过重样本进行定向重写。当前论文版数据中，共有 `23` 条主测试集样本被重写，重写后审计优先级已从原始版本的 `29 low / 7 medium / 16 high` 改进为 `52 low / 0 medium / 0 high`。

这种数据组织方式使本文能够同时满足两类需求：一方面，系统在运行时只接触标准 benchmark 输入，避免构造元信息泄漏；另一方面，研究者仍可以在论文写作和复查阶段追溯每条样本的来源、重写历史和控制变量。

== 评价指标设计

本文采用自动指标与人工判断相结合的评价方式。

自动指标方面，本文重点关注以下几类错误。其一是 `policy_composite_error`，用于衡量系统输出的干预状态与升级判断是否与 gold 标签不一致；其二是 `policy_over_intervention_rate`，用于检验系统是否在需要澄清或共同规划的场景中过早推进执行；其三是 `policy_under_help_rate`，用于检验系统是否在适合执行的场景中过度保守；其四是 `escalation_error_rate`，用于评估系统在高风险样本上是否错误地忽视升级或误触发升级。

人工评估方面，本文保留盲评数据包导出能力，用于补充自动标签不足。评审重点包括：回复是否与用户当前状态和任务阶段相匹配、是否存在明显越界或误判、以及系统是否只是改变语气而没有改变实际干预方式。

从研究问题映射关系来看，研究问题一主要依赖主测试集上的自动策略指标与人工合适性判断；研究问题二主要依赖中性集上的帮助能力与保守错误率；研究问题三则需要结合 paired variant 分析，观察系统是否随着 affect 和任务变量变化而稳定切换干预模式。

== 系统功能与性能验证

除了机制层比较实验，本文还需要从系统层验证桌面宠物系统原型的闭环可用性。当前仓库中已经提供了相应的观察模板与总结脚本，正式实验时可从以下三个维度记录结果。

第一，功能闭环验证。需要确认主聊天窗口、记忆检索、策略选择、模型回复、工具审批与 companion 外显反馈能够在统一线程中正常协同工作。

第二，性能指标记录。需要采集平均响应时延、affect 聚合成本、记忆检索开销、动作反馈延迟以及基础资源占用，以便说明系统在桌面端具备可运行性。

第三，运行稳定性检查。需要验证在不同实验条件、不同模型配置和不同线程长度下，系统是否仍能保持一致的上下文装配流程与策略输出语义。

本节正式写作时，建议补入系统层观察文件、基准运行目录和实际测得的延迟数据，并形成对应图表或表格。

== 显式情感策略对比实验与消融分析

机制层实验的核心是比较 `no_affect`、`tone_only` 与 `explicit_policy` 三种条件在主测试集上的表现差异。

如果 `explicit_policy` 在主测试集上的 `policy_composite_error`、`policy_over_intervention_rate` 和 `escalation_error_rate` 低于前两种条件，就可以支持“显式策略层有助于减少不当干预”的结论。如果它仅仅在回复风格上更温和，却没有降低上述错误率，那么本文的核心命题就无法成立。

本节建议在正式跑表后补入以下内容：总体结果表、按变体类型切分的错误率、典型失败案例、以及对 `runtime_policy_applied` 字段的审计结果。必要时，还可以补充对强 baseline 的讨论，例如“prompt+skill”条件是否已经足够强，从而验证显式策略层是否真的带来了额外收益。

== 中性任务非劣性分析与案例讨论

为了证明 affect-aware policy 并不会让系统在所有任务上都变得过度谨慎，本文还需要在中性对照集上分析其非劣性。

理想情况下，`explicit_policy` 条件在中性任务上的 `policy_under_help_rate` 不应明显高于 `no_affect` 或 `tone_only`，其实际输出的任务可用性也不应显著下降。如果系统在普通改写、清单生成、信息整理等场景中普遍变慢、变保守或频繁追问无关问题，那么说明策略层的收益是以整体可用性退化为代价换来的。

此外，本节还适合加入若干案例讨论。通过对同一基础任务在不同 variant 下的对比，可以更清楚地说明 affect 是如何改变系统干预姿态的；而通过中性任务案例，则可以验证系统是否能在不需要情感感知时保持干净、直接的任务协作风格。

== 本章小结

本章对论文实验的目标、条件、数据集、评价指标与分析框架进行了统一整理。当前初稿已经将 thesis benchmark、系统层评测模板与三条件比较逻辑整合到可直接继续写作的结构中。后续只需在正式跑表后补入具体结果、图表和案例分析，即可将本章从“实验设计稿”推进为“完整实验结果章”。
