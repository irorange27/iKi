= 相关理论与关键技术

== Agent 系统与大语言模型基础

大语言模型通过海量语料预训练获得了较强的语言理解和生成能力，使系统能够在开放输入条件下进行对话、改写、摘要、分类和解释等多种任务。然而，单纯的文本生成并不自动构成一个完整的 agent。真正意义上的 agent 通常还需要具备目标感知、状态维护、行动选择、外部执行和结果回收等能力。换言之，agent 不是“更会聊天的模型”，而是“以模型为核心、围绕任务闭环组织起来的软件系统”。

从研究脉络来看，ReAct@yao2023react 通过将推理与行动交替组织，使模型可以在复杂任务中边思考边操作；Toolformer@schick2023toolformer 则说明模型可以学习在合适的时候借助外部工具。这些研究共同推动了大语言模型从静态文本引擎向可执行智能体发展，也表明高质量 agent 往往不是依赖单一 prompt 完成，而是依赖模型、状态、工具和反馈共同构成执行循环。

从系统结构角度看，agent 至少包含三个基本要素。第一是任务表示，即系统是否知道当前要解决什么问题；第二是状态表示，即系统是否知道自己已经做到了哪一步、还缺什么信息；第三是动作空间，即系统能否在回答、澄清、规划、调用工具和等待反馈等动作之间做出选择。本文所研究的桌面宠物系统正是在这一意义上理解 agent：其核心不是角色拟人化，而是让系统在桌面环境中具备围绕任务和用户状态持续行动的能力。

对于本文而言，这一理论基础尤为重要。因为本文后续提出的 affect-aware policy，本质上也是对 agent 动作空间与状态表示的进一步细化。系统不再只是在“生成一句回答”，而是在“根据状态选择何种干预方式后再生成回答”。因此，第二章讨论 agent 的意义，并不是为了介绍通用概念，而是为了为第 4 章的显式策略层提供理论承接。

== 工具调用与多模型编排技术

工具调用技术使大语言模型能够突破仅依赖内部参数回答问题的局限，将语言生成与外部环境中的可执行资源连接起来。在桌面端 personal agent 场景中，工具调用的范围尤为广泛，不仅包括文件读写、命令执行、网页访问、数据库查询和 API 请求，还包括技能装载、任务调度、后续提醒和受控子 agent 委派等能力。此时，工具调用不再是单纯的“函数触发”，而是一个涉及权限、边界、时机和恢复策略的系统性问题。

从实现角度看，工具调用至少包含四个关键环节：识别是否需要工具、选择合适工具、约束工具可访问范围，以及处理执行结果与异常恢复。在高自治系统中，如果这些环节全部交给主模型隐式完成，往往会出现权限边界模糊、执行不透明和错误恢复困难等问题。因此，现代 agent 系统越来越强调工具接口的类型化、审批机制和工作区边界控制。本文系统后续采用的显式审批、工作区约束和 affect guardrail，正是建立在这一认识之上的。

另一方面，实际系统中的能力往往并不适合由同一个模型承担。主对话模型需要保证表达质量和任务协作能力，工具选择模型更强调控制成本和决策速度，情感分析模型则需要稳定输出结构化状态。因此，多模型编排成为工程上的自然选择。通过多模型分工，系统可以在性能、成本与能力边界之间取得平衡，同时避免把所有职责堆叠到一个高成本模型上。

不过，多模型编排也带来新的设计要求。不同模型之间必须共享一致的状态语义，否则主对话模型和辅助模型会在同一线程上形成“各自理解一套世界”的问题；同时，多模型体系也必须接受统一的边界控制，否则辅助模型容易在不可见处引入不一致的行为逻辑。本文系统采用统一的运行时 metadata、结构化 AffectSignal 和显式策略状态，就是为了解决这种多模型协同中的一致性问题。

== 上下文组装与记忆机制

持续多轮交互对上下文管理提出了比单轮问答更高的要求。随着对话长度增加，系统需要在有限上下文窗口内平衡近期历史、线程摘要、长期记忆、任务目标、技能说明和 affect 状态等多种信息来源。如果这些内容只是线性拼接进 prompt，就容易出现输入膨胀、信息重复、冲突加剧和推理漂移等问题。因此，上下文组装不应被视为提示词拼接技巧，而应被视为 agent 系统中的一条独立能力链路。

从结构上看，上下文组装至少包含三个核心问题。其一是“装什么”，即系统需要决定哪些信息块值得在当前轮进入模型输入；其二是“装多少”，即如何在有限预算下控制不同信息块的长度和优先级；其三是“为何被装入或舍弃”，即上下文装配过程是否可观测、可解释、可测试。本文所依赖的实现将上下文分解为 `identity`、`thread-summary`、`memory`、`affect` 和 `skills` 等逻辑块，并为每个块记录状态、估计长度和裁剪原因，这使上下文装配从隐式 prompt 堆叠转变为可审计的结构化过程。

记忆机制与上下文组装密切相关，但两者并不等价。上下文组装关注的是“当前轮模型应看到什么”，而记忆机制关注的是“系统长期保留了什么，以及何时值得取回”。对于 persistent agent，真正有价值的记忆并不是所有历史对话的堆积，而是对用户偏好、约束条件、持续任务、已做决策和敏感边界等信息的有选择沉淀。因此，记忆系统通常需要区分即时上下文、线程摘要、长期归档和带证据的连续性事实等不同层次。

本文系统在这一点上采取了较为严格的边界设计。系统优先在线程内检索相关信息，避免无约束跨线程扩散；在记忆写入和检索时考虑隐私模式、工作区边界和配置开关，防止系统把本应短暂存在的信息错误地长期保存。更重要的是，记忆机制并不独立于策略层运行：在某些边界敏感或高不确定状态下，是否继续写入或强化某类记忆，也应受显式策略控制。

== 情感计算与用户状态建模方法

情感计算关注如何从文本、语音、行为和生理等信号中识别用户状态，并将这种状态用于交互优化。在对话系统中，情感建模通常存在两类主流思路：一种是离散标签表示，例如愤怒、悲伤、焦虑、平静等，用于描述较直观的情绪类别；另一种是维度表示，例如价度和唤醒度，用于反映情绪的正负方向和激活强弱。GoEmotions@demszky2020goemotions 和 DailyDialog@li2017dailydialog 等工作说明，文本中的情绪线索可以通过更细粒度的标签体系和对话语境来建模，而不只是简单的正负二分类。

对于本文场景而言，仅使用离散标签存在明显不足。因为桌面任务推进中的关键问题，很多时候并不在于用户究竟是“悲伤”还是“愤怒”，而在于用户是否准备好行动、是否处于高激活状态、是否缺乏关键信息、以及是否已经接近不宜继续由系统高自治介入的边界。因此，本文采用的是“离散标签 + 维度状态 + 时间聚合”的混合表示思路。系统既保留主情绪标签和置信度，也关注价度、唤醒度、样本窗口和时间衰减结果，从而形成更适合任务协同的 affect belief。

本文特别强调，affect 不是用户真实心理状态的真值，而是系统根据有限文本证据对当前用户状态做出的带不确定性的估计。把 affect 视为 belief state 而非 ground truth，具有重要方法论意义。其一，它避免系统在论文叙事和运行时行为上过度解释用户；其二，它允许系统在证据不足时保持克制，而不是被迫给出过强判断；其三，它使 affect 与任务上下文、对话阶段和策略层能够形成条件组合关系，而不是单独决定系统行为。

在此基础上，本文所关注的并不是“模型有没有情绪”，而是“情感信号是否能帮助系统更稳地选择干预模式”。因此，情感建模在本文中并不是终点，而是进入显式干预策略层的前置状态表示。

== 桌面宠物交互与 Electron 桌面应用实现技术

桌面宠物交互系统既是一种界面形式，也是一种系统状态外显方式。与普通聊天窗口不同，桌宠强调持续在场、低打扰可见性和轻量状态反馈。它不必始终要求用户进入主对话窗口，却需要在关键时刻让用户感知系统正在思考、等待、执行还是处于更克制的承接姿态。因此，桌宠不是单纯的视觉装饰，而是桌面智能体的一个外显交互层。

从实现技术看，桌面端 personal agent 需要同时处理窗口管理、本地资源访问、进程间通信、配置持久化和多视图交互等问题。Electron 作为跨平台桌面应用框架，能够在主进程中统一管理窗口、IPC、权限边界和运行时资源，在渲染层承载聊天界面、设置界面和 companion 视图，并与本地数据库、文件系统和外部守护进程形成稳定协同。对于本文系统而言，Electron 的价值不只是“能做桌面界面”，而是它天然适合作为桌面控制平面，把系统的可视交互层和本地运行时有机连接起来。

另一方面，桌宠交互的设计还涉及“外显层应承担多少智能”。如果把 companion 本身做成第二个会自由生成的 agent，就会迅速增加系统耦合和不可审计性。因此，本文采用轻量 companion pebble 方案：外显层只负责显示主运行时裁决后的 phase，而不重新生成自己的智能决策逻辑。系统将内部状态映射为有限的 `thinking`、`stabilize`、`clarify`、`co_plan`、`execute` 等 phase，并以固定映射方式表现，从而兼顾低打扰体验与系统一致性。

这种实现思路对论文同样重要。它使“桌面宠物交互系统”不只是界面标题，而是和内部运行时策略形成稳定闭环：内部 affect-aware policy 改变系统姿态，外部 companion 则让这一姿态被用户看见。

== 本章小结

本章从 Agent 系统、大语言模型、工具调用、多模型编排、上下文与记忆机制、情感计算以及桌面宠物交互实现等方面，系统梳理了本文所依赖的理论基础与关键技术。可以看到，本文并不是在现有聊天系统外部简单附加一个桌宠界面，而是在 agent 执行能力、情感状态建模和桌面外显交互三条技术脉络的交叉点上，尝试构建一个更适合真实桌面协同场景的持续存在型智能体系统。后续章节将在这些基础上进一步展开系统需求、总体架构与关键机制实现分析。
