block-exotic-subdeps=false
# Left over from when the lockfile recorded an SSH URL for @electron/node-gyp,
# which fails where port 22 is blocked. The dependency is HTTPS now.
verify-deps-before-run=false
